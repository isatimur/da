{
  "name": "daily-affirmations-extension",
  "version": "1.2.3",
  "description": "Chrome extension for Daily Affirmations with Breathing Exercises and Task Manager",
  "scripts": {
    "build": "node build.js",
    "build:css": "node -e \"require('./build.js').bundleCSS()\"",
    "build:minify": "node -e \"require('./build.js').minifyCSS()\"",
    "dev": "node build.js && echo 'Development build complete'",
    "prod": "NODE_ENV=production node build.js"
  },
  "devDependencies": {
    "crypto": "^1.0.1"
  }
} 